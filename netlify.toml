[build]
  command = "npm run build"
  # publish 디렉토리는 @netlify/plugin-nextjs가 자동 설정
  # (publish=.next 로 명시하면 SSR 라우트가 404가 됨)

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NEXT_TELEMETRY_DISABLED = "1"
